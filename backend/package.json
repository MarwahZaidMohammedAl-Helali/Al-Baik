{
  "name": "al-baik-api",
  "version": "1.0.0",
  "description": "Al-Baik E-commerce API with Cloudflare Workers + Turso",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate:sqlite",
    "db:migrate": "drizzle-kit push:sqlite"
  },
  "dependencies": {
    "@libsql/client": "^0.4.0",
    "drizzle-orm": "^0.29.0",
    "hono": "^3.11.0",
    "zod": "^3.22.0",
    "@hono/zod-validator": "^0.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231025.0",
    "drizzle-kit": "^0.20.0",
    "wrangler": "^3.15.0"
  }
}